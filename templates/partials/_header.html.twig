<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home.index') }}">
      <img class="logo" src="{{ asset('build/images/Vector.png') }}" alt="ACME logo">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav m-auto">
        <li class="nav-item">
          <a class="nav-link nav__link" href="{{ path('home.index') }}">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav__link" href="{{ path('carte.index') }}">La carte</a>
        </li>

        <li class="nav-item">
          <a class="nav-link nav__link" href="{{ path('reservation.index') }}">Réservations</a>
        </li>
      </ul>

      <div class="d-flex">
        <ul class="navbar-nav me-auto">
  
          {% if app.user %}
          <li class="nav-item dropdown" style="margin-right: 8rem;">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true">
              {{ app.user.name }}
              {{ app.user.lastname }}
            </a>
            <div class="dropdown-menu">
<a class="dropdown-item" href="{{ path('my_reservation.index', {id : app.user.id}) }}">Mes réservations</a>              </a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Modifier mes informations
              </a>
              <a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id}) }}">Modifier mon mot de passe
              </a>
              <div class="dropdown-divider"></div>
              {% if 'ROLE_ADMIN' in app.user.roles %}
              <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
              <div class="dropdown-divider"></div>
              {% endif %}
              <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
            </div>
          </li>
          {% else %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('security.login') }}">Se connecter</a>
            </li>
          {% endif %}

        </ul>
      </div>
    </div>
  </div>
</nav>

<script>
// Récupérer tous les liens dans le menu de navigation
const links = document.querySelectorAll("nav ul li a");

// Parcourir tous les liens et ajouter un écouteur d'événements
links.forEach((link) => {
  link.addEventListener("click", (event) => {
    // Supprimer la classe "active" de tous les liens
    links.forEach((link) => {
      link.classList.remove("active");
    });

    // Ajouter la classe "active" au lien cliqué
    link.classList.add("active");
  });
});
console.log(links);

// Ajouter la classe "active" au lien de la page actuelle
const currentUrl = window.location.href;
links.forEach((link) => {
  if (link.href === currentUrl) {
    link.classList.add("active");
  }
});

</script>